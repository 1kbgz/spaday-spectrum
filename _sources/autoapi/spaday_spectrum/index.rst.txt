spaday_spectrum
===============

.. py:module:: spaday_spectrum


Submodules
----------

.. toctree::
   :maxdepth: 1

   /autoapi/spaday_spectrum/button/index
   /autoapi/spaday_spectrum/checkbox/index
   /autoapi/spaday_spectrum/switch/index
   /autoapi/spaday_spectrum/tabs/index
   /autoapi/spaday_spectrum/textfield/index
   /autoapi/spaday_spectrum/theme/index


Attributes
----------

.. autoapisummary::

   spaday_spectrum.__version__
   spaday_spectrum.package


Classes
-------

.. autoapisummary::

   spaday_spectrum.SpButton
   spaday_spectrum.SpClearButton
   spaday_spectrum.SpCloseButton
   spaday_spectrum.SpCheckbox
   spaday_spectrum.SpSwitch
   spaday_spectrum.SpTab
   spaday_spectrum.SpTabPanel
   spaday_spectrum.SpTabs
   spaday_spectrum.SpTabsOverflow
   spaday_spectrum.SpTextfield
   spaday_spectrum.SpTheme


Package Contents
----------------

.. py:class:: SpButton(*children: spaday.component.Child, key: str | None = None, pending_label: str | None = None, pending: bool | None = None, variant: Any = None, static_color: Literal['black', 'white'] | None = None, treatment: Any = None, quiet: bool | None = None, no_wrap: bool | None = None, active: bool | None = None, type: Literal['button', 'submit', 'reset'] | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-button'



.. py:class:: SpClearButton(*children: spaday.component.Child, key: str | None = None, label: str | None = None, quiet: bool | None = None, variant: Literal['overBackground'] | None = None, static_color: Literal['white'] | None = None, active: bool | None = None, type: Literal['button', 'submit', 'reset'] | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-clear-button'



.. py:class:: SpCloseButton(*children: spaday.component.Child, key: str | None = None, variant: Any = None, static_color: Literal['black', 'white'] | None = None, active: bool | None = None, type: Literal['button', 'submit', 'reset'] | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-close-button'



.. py:class:: SpCheckbox(*children: spaday.component.Child, key: str | None = None, disabled: bool | None = None, indeterminate: bool | None = None, invalid: bool | None = None, emphasized: bool | None = None, tabindex: float | None = None, checked: bool | None = None, name: str | None = None, readonly: bool | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-checkbox'



.. py:class:: SpSwitch(*children: spaday.component.Child, key: str | None = None, emphasized: bool | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-switch'



.. py:class:: SpTab(*children: spaday.component.Child, key: str | None = None, disabled: bool | None = None, label: str | None = None, selected: bool | None = None, vertical: bool | None = None, value: str | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-tab'



.. py:class:: SpTabPanel(*children: spaday.component.Child, key: str | None = None, selected: bool | None = None, value: str | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-tab-panel'



.. py:class:: SpTabs(*children: spaday.component.Child, key: str | None = None, auto: bool | None = None, compact: bool | None = None, dir: Literal['ltr', 'rtl'] | None = None, direction: Literal['vertical', 'vertical-right', 'horizontal'] | None = None, emphasized: bool | None = None, label: str | None = None, enableTabsScroll: bool | None = None, quiet: bool | None = None, selected: str | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-tabs'



.. py:class:: SpTabsOverflow(*children: spaday.component.Child, key: str | None = None, compact: bool | None = None, label_previous: str | None = None, label_next: str | None = None, dir: Literal['ltr', 'rtl'] | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-tabs-overflow'



.. py:class:: SpTextfield(*children: spaday.component.Child, key: str | None = None, value: Any = None, allowed_keys: str | None = None, focused: bool | None = None, invalid: bool | None = None, label: str | None = None, name: str | None = None, placeholder: str | None = None, type: Any = None, pattern: str | None = None, grows: bool | None = None, maxlength: float | None = None, minlength: float | None = None, multiline: bool | None = None, readonly: bool | None = None, rows: float | None = None, valid: bool | None = None, quiet: bool | None = None, required: bool | None = None, autocomplete: Any = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-textfield'



.. py:class:: SpTheme(*children: spaday.component.Child, key: str | None = None, color: Literal['lightest', 'light', 'dark', 'darkest', ''] | None = None, scale: Literal['medium', 'large', ''] | None = None, lang: str | None = None, dir: Literal['ltr', 'rtl', ''] | None = None, system: Literal['spectrum', 'express'] | None = None, **props: Any)

   Bases: :py:obj:`spaday.component.Component`


   Base for a node in the spaday component tree.

   Author it two equivalent ways: nest children **positionally** in the constructor and set generic
   props as keywords — ``App(Nav("title"), Body(...), id="root")`` — or build it up fluently with
   ``.child()`` / ``.prop()``. A string child becomes a text node. Subclasses set the class attribute
   ``tag`` and forward their typed props via ``props=`` (only the ones the author set — ``None`` means
   "leave the element's own default").


   .. py:attribute:: tag
      :value: 'sp-theme'



.. py:data:: __version__
   :value: '0.1.0'


.. py:data:: package

